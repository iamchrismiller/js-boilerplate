/*global define, require*/

define(function (require) {
  "use strict";

  return {
    mainLayout : require('tpl!../templates/mainLayout.tpl'),
    headerLayout : require('tpl!../templates/headerLayout.tpl')
  };
});
